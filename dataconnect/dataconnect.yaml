specVersion: "v1beta"
serviceId: "event-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "event-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
